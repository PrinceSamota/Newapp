<% content_for(:title, "Order Management") %>
<%= link_to order_entries_path, class: "flex items-center gap-2 text-black hover:bg-gray-200 rounded-md p-2 w-22 border border-gray-300" do %>
  <i class="fa-solid fa-arrow-left"></i>
  <span class="font-bold">Back</span>
<% end %>

<% if @order_entry.deleted_at.present? %>
  <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-6 flex items-center">
    <i class="fa-solid fa-exclamation-triangle mr-2"></i>
    <span class="font-semibold">This order has been deleted and cannot be modified.</span>
  </div>
<% end %>

<%= form_with(model: @order_entry, local: true) do |f| %>
    <div class="max-w-[100%] mx-auto mt-10 bg-white border border-gray-200 rounded-2xl shadow p-10 space-y-6">
      <h2 class="text-2xl font-bold text-gray-800">Edit Order</h2>
     
      <%= link_to new_product_label_labels_path(order_id: @order_entry.id), 
          class: "flex items-center w-max gap-2 text-white bg-purple-600 hover:bg-purple-700 rounded-md px-4 py-2 border shadow-md transition duration-200 mb-4" do %>
        <i class="fa-solid fa-tag"></i>
        <span class="font-bold">Create Sticker</span>
      <% end %>

      <%= link_to generate_qr_links_order_entry_path(@order_entry), 
          class: "flex items-center w-max gap-2 text-white bg-green-600 hover:bg-green-700 rounded-md px-4 py-2 border shadow-md transition duration-200 mb-4" do %>
        <i class="fa-solid fa-qrcode"></i>
        <span class="font-bold">Generate Serial No QR Links</span>
      <% end %>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <!-- Row 1 -->
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Order No</label>
          <%= f.text_field :order_no, 
              readonly: @order_entry.deleted_at.present?,
              class: "w-full rounded-xl border border-gray-300 p-2.5 #{'bg-gray-100 cursor-not-allowed' if @order_entry.deleted_at.present?} focus:ring-blue-500 focus:border-blue-500" %>
        </div>
  
        <div>
        <div class="relative">
  <label class="block text-sm font-medium text-gray-700 mb-1">Article No</label>

  <input type="text"
    id="articleSearchInput"
    value="<%= @order_entry.article_no %>"
    onkeyup="filterDropdown()"
    class="w-full rounded-xl border border-gray-300 p-2.5 #{'bg-gray-100 cursor-not-allowed' if @order_entry.deleted_at.present?}" />

  <!-- Dropdown -->
  <div id="articleDropdownList"
    class="absolute z-10 w-full bg-white border border-gray-300 rounded-lg mt-1 max-h-60 overflow-auto hidden">

    <% @item_masters.each do |item| %>
      <div 
        class="px-4 py-2 cursor-pointer hover:bg-gray-100"
        onclick="selectArticle(this)"
        data-article="<%= item.article_number %>"
        data-sku="<%= item.sku_id %>"
        data-fuse="<%= item.fuse_type&.name %>"
        data-loop="<%= item.loop&.name %>"
        data-item-type="<%= item.item_type&.name %>"
        data-profile="<%= item.profile&.name %>"
        data-wattage="<%= item.wattage&.name %>"
        data-voltage="<%= item.voltage&.name %>"
        data-length="<%= item.length&.name %>"
        data-cct="<%= item.cct&.name %>"
        data-cover-type="<%= item.cover_type&.name %>"
        data-extra="<%= item.extra&.name %>">
        <%= item.article_number %>
      </div>
    <% end %>
  </div>

  <input type="hidden" name="order_entry[article_no]" id="selectedArticle" value="<%= @order_entry.article_no %>">
</div>
        </div>
  
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Client</label>
          <%= f.collection_select :client_id, Client.all, :id, :company_name, 
              { prompt: 'Select Client' }, 
              { disabled: @order_entry.deleted_at.present?,
                class: "w-full rounded-xl border border-gray-300 p-2.5 #{'bg-gray-100 cursor-not-allowed' if @order_entry.deleted_at.present?} focus:ring-blue-500 focus:border-blue-500" } %>
        </div>
  
        <!-- Row 2 -->
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Target Date</label>
          <%= f.date_field :target_date, 
              readonly: @order_entry.deleted_at.present?,
              class: "w-full rounded-xl border border-gray-300 p-2.5 #{'bg-gray-100 cursor-not-allowed' if @order_entry.deleted_at.present?} focus:ring-blue-500 focus:border-blue-500" %>
        </div>
  
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Ship To Location</label>
          <%= f.collection_select :location_id, Location.all, :id, :name, 
              { prompt: 'Select Location' }, 
              { disabled: @order_entry.deleted_at.present?,
                class: "w-full rounded-xl border border-gray-300 p-2.5 #{'bg-gray-100 cursor-not-allowed' if @order_entry.deleted_at.present?} focus:ring-blue-500 focus:border-blue-500" } %>
        </div>
  
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Quantity</label>
          <%= f.number_field :qty, 
              readonly: @order_entry.deleted_at.present?,
              readonly: true,
              class: "w-full rounded-xl border border-gray-300 p-2.5 #{'bg-gray-100 cursor-not-allowed' if @order_entry.deleted_at.present?} focus:ring-blue-500 focus:border-blue-500" %>
        </div>
  
        <!-- Full width read-only field -->
        <div class="md:col-span-1">
          <label class="block text-sm font-medium text-gray-700 mb-1">SKU Number</label>
          <%= f.text_field :sku_number, id: "sku_number", readonly: true, class: "w-full rounded-xl border border-gray-300 p-2.5 bg-gray-100 text-gray-700 cursor-not-allowed" %>
        </div>
        <!-- Row: Decoded Article Fields -->
<div>
  <label class="block text-sm font-medium text-gray-700 mb-1">Fuse Type</label>
  <%= f.text_field :fuse_type, id: "fuse_type", readonly: true, class: "w-full rounded-xl border border-gray-300 p-2.5 bg-gray-100 text-gray-700 cursor-not-allowed" %>
</div>

<div>
  <label class="block text-sm font-medium text-gray-700 mb-1">Loop</label>
  <%= f.text_field :loop, id: "loop", readonly: true, class: "w-full rounded-xl border border-gray-300 p-2.5 bg-gray-100 text-gray-700 cursor-not-allowed" %>
</div>

<div>
  <label class="block text-sm font-medium text-gray-700 mb-1">Item Type</label>
  <%= f.text_field :item_type, id: "item_type", readonly: true, class: "w-full rounded-xl border border-gray-300 p-2.5 bg-gray-100 text-gray-700 cursor-not-allowed" %>
</div>

<div>
  <label class="block text-sm font-medium text-gray-700 mb-1">Profile</label>
  <%= f.text_field :profile, id: "profile", readonly: true, class: "w-full rounded-xl border border-gray-300 p-2.5 bg-gray-100 text-gray-700 cursor-not-allowed" %>
</div>

<div>
  <label class="block text-sm font-medium text-gray-700 mb-1">Wattage</label>
  <%= f.text_field :wattage, id: "wattage", readonly: true, class: "w-full rounded-xl border border-gray-300 p-2.5 bg-gray-100 text-gray-700 cursor-not-allowed" %>
</div>

<div>
  <label class="block text-sm font-medium text-gray-700 mb-1">Voltage</label>
  <%= f.text_field :voltage, id: "voltage", readonly: true, class: "w-full rounded-xl border border-gray-300 p-2.5 bg-gray-100 text-gray-700 cursor-not-allowed" %>
</div>

<div>
  <label class="block text-sm font-medium text-gray-700 mb-1">Length</label>
  <%= f.text_field :length, id: "length", readonly: true, class: "w-full rounded-xl border border-gray-300 p-2.5 bg-gray-100 text-gray-700 cursor-not-allowed" %>
</div>

<div>
  <label class="block text-sm font-medium text-gray-700 mb-1">CCT</label>
  <%= f.text_field :cct, id: "cct", readonly: true, class: "w-full rounded-xl border border-gray-300 p-2.5 bg-gray-100 text-gray-700 cursor-not-allowed" %>
</div>

<div>
  <label class="block text-sm font-medium text-gray-700 mb-1">Cover Type</label>
  <%= f.text_field :cover_type, id: "cover_type", readonly: true, class: "w-full rounded-xl border border-gray-300 p-2.5 bg-gray-100 text-gray-700 cursor-not-allowed" %>
</div>

<!-- Start Serial No -->
<div>
  <label class="block text-sm font-medium text-gray-700 mb-1">Start Serial No</label>
  <%= f.text_field :start_serial_no,
      readonly: @order_entry.start_serial_no.present?,
      class: "w-full rounded-xl border border-gray-300 p-2.5 #{ @order_entry.start_serial_no.present? ? 'bg-gray-100 text-gray-700 cursor-not-allowed' : '' }" %>
</div>

<!-- End Serial No -->
<div>
  <label class="block text-sm font-medium text-gray-700 mb-1">End Serial No</label>
  <%= f.text_field :end_serial_no,
      readonly: @order_entry.end_serial_no.present?,
      class: "w-full rounded-xl border border-gray-300 p-2.5 #{ @order_entry.end_serial_no.present? ? 'bg-gray-100 text-gray-700 cursor-not-allowed' : '' }" %>
</div>

<!-- MFG Date -->
<div>
  <label class="block text-sm font-medium text-gray-700 mb-1">MFG Date</label>
  <%= f.date_field :mfg_date, 
      readonly: @order_entry.deleted_at.present?,
      class: "w-full rounded-xl border border-gray-300 p-2.5 #{'bg-gray-100 cursor-not-allowed' if @order_entry.deleted_at.present?} focus:ring-blue-500 focus:border-blue-500" %>
</div>


<div>
  <label class="block text-sm font-medium text-gray-700 mb-1">
    Order Receiving Date
  </label>

  <%= f.date_field :order_receiving_date,
      value: @order_entry.created_at.strftime("%Y-%m-%d"),
      readonly: @order_entry.deleted_at.present?,
      class: "w-full rounded-xl border border-gray-300 p-2.5 #{'bg-gray-100 cursor-not-allowed' if @order_entry.deleted_at.present?} focus:ring-blue-500 focus:border-blue-500" %>
</div>


<!-- Driver Revision No -->
<% if @order_entry.driver_revision_id.present? %>
  <div>
    <label class="block text-sm font-medium text-gray-700 mb-1">Driver Revision No</label>
    <%= form_with model: @order_entry, local: true do |f| %>
      <%= f.collection_select :driver_revision_id, 
          DriverRevision.where(org_id: current_user.org_id), 
          :id, :name, 
          { prompt: 'Select Revision' }, 
          { disabled: @order_entry.deleted_at.present?,
            class: "w-full rounded-xl border border-gray-300 p-2.5 focus:ring-blue-500 focus:border-blue-500 #{'bg-gray-100 cursor-not-allowed' if @order_entry.deleted_at.present?}" } %>
    <% end %>
  </div>
<% else %>
  <div>
    <%= render partial: "uploads/driver_dropdown", locals: {
      selected_driver_revision_id: @order_entry.driver_revision_id,
      show_form: false,
      driver_revision_errors: []
    } %>
  </div>
<% end %>

<!-- Dispatch No -->
<div>
  <label class="block text-sm font-medium text-gray-700 mb-1">Dispatch No</label>
   <%= f.object.dispatch_d_id || "—" %>
</div>

<!-- Status -->
<div>
  <label class="block text-sm font-medium text-gray-700 mb-1">Status</label>
  <%= f.text_field :status, 
      readonly: @order_entry.deleted_at.present?,
      class: "w-full rounded-xl border border-gray-300 p-2.5 #{'bg-gray-100 cursor-not-allowed' if @order_entry.deleted_at.present?}" %>
</div>

<div>
  <label class="block text-sm font-medium text-gray-700 mb-1">Number Of Box</label>
  <%= f.text_field :box, 
      readonly: @order_entry.deleted_at.present?,
      class: "w-full rounded-xl border border-gray-300 p-2.5 #{'bg-gray-100 cursor-not-allowed' if @order_entry.deleted_at.present?}" %>
</div>

<div>
  <label class="block text-sm font-medium text-gray-700 mb-1">Extra</label>
  <%= f.text_field :extra, id: "extra", readonly: true, class: "w-full rounded-xl border border-gray-300 p-2.5 bg-gray-100 text-gray-700 cursor-not-allowed" %>
</div>

<div>
  <label class="block text-sm font-medium text-gray-700 mb-1">Remark</label>
  <%= f.text_field :remark, 
      readonly: @order_entry.deleted_at.present?,
      class: "w-full rounded-xl border border-gray-300 p-2.5 #{'bg-gray-100 cursor-not-allowed' if @order_entry.deleted_at.present?}" %>
</div>

<% if f.object.start_serial_no.blank? && f.object.end_serial_no.blank? %>
  <div>
    <%= f.label :generate_sno, "Generate SNO.", class: "block text-sm font-medium text-gray-700 mb-1" %>
    <%= f.select :generate_sno, [["Yes", true], ["No", false]], 
          { prompt: "Select Option", selected: f.object.generate_sno }, 
          { disabled: @order_entry.deleted_at.present?,
            class: "required-field w-full border border-gray-300 rounded-lg px-4 py-2 #{'bg-gray-100 cursor-not-allowed' if @order_entry.deleted_at.present?}" } %>
  </div>
<% end %>

      </div>
  

        
        <% if @order_entry.deleted_at.present? %>
          <button type="button" disabled class="inline-flex items-center px-4 py-2 bg-gray-400 text-white text-sm font-medium rounded-xl cursor-not-allowed opacity-50">
            Order Deleted - Cannot Update
          </button>
        <% else %>
          <%= f.submit "Update Order", class: "inline-flex items-center px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white text-sm font-medium rounded-xl transition duration-200 cursor-pointer" %>
        <% end %>
      </div>
    
    </div>
  <% end %>
  
<script>
function filterDropdown() {
  const input = document.getElementById("articleSearchInput").value.toLowerCase();
  const dropdown = document.getElementById("articleDropdownList");

  dropdown.classList.remove("hidden");

  const items = dropdown.querySelectorAll("div");
  items.forEach(item => {
    const article = item.dataset.article || "";
    item.style.display = article.toLowerCase().includes(input) ? "block" : "none";
  });
}

function selectArticle(el) {
  // Set article
  document.getElementById("articleSearchInput").value = el.dataset.article || "";
  document.getElementById("selectedArticle").value = el.dataset.article || "";

  // ✅ SAFE AUTO FILL (no error if field missing)
  setValue("sku_number", el.dataset.sku);
  setValue("fuse_type", el.dataset.fuse);
  setValue("loop", el.dataset.loop);
  setValue("item_type", el.dataset.itemType);
  setValue("profile", el.dataset.profile);
  setValue("wattage", el.dataset.wattage);
  setValue("voltage", el.dataset.voltage);
  setValue("length", el.dataset.length);
  setValue("cct", el.dataset.cct);
  setValue("cover_type", el.dataset.coverType);
  setValue("extra", el.dataset.extra);

  // hide dropdown
  document.getElementById("articleDropdownList").classList.add("hidden");
}

// helper function (IMPORTANT)
function setValue(id, value) {
  const el = document.getElementById(id);
  if (el) {
    el.value = value || "";
  }
}
</script>